///
///  Copyright (c) 2021 Tjek. All rights reserved.
///

import UIKit
import TjekAPI

public struct TjekPublicationReader {
    public struct Config {
        
    }
    
    /// You must initialize the TjekAPI before calling this function, unless you override the `api` parameter.
    public static func initialize(config: Config, api: TjekAPI = .shared) {
        
    }
    /// You must initialize the TjekAPI before calling this function, unless you override the `api` parameter.
    public static func initialize(api: TjekAPI = .shared) {
        
    }
}
